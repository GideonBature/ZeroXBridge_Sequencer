[package]
name = "tree-builder"
version = "0.1.0"
edition = "2024"

[dependencies]
accumulators = { version = "0.5.0", features = ["all"] }
async-trait = "0.1"
tokio = { version = "1", features = ["full"] }
thiserror = "1.0"
hex = "0.4"
num-bigint = "0.4"
num-traits = "0.2"

[dev-dependencies]
hex = "0.4"
